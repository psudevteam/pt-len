import { NextPage } from "next";
import { ReactElement } from "react";

const LoadingDashboardPage: NextPage = (): ReactElement => {
  return <div>Loading...</div>;
};

export default LoadingDashboardPage;
